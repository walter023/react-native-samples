import { Layout } from '../layout';
import { BackButton } from '../components/BackButton';

export const ShaderScreen: React.FC = () => {
  return (
    <Layout>
      <BackButton />
      {/*  TODO:  <ShaderComponent /> */}
    </Layout>
  );
};
