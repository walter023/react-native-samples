import { Layout } from '../layout';
import { BackButton } from '../components/BackButton';

export const VectorReflectionScreen: React.FC = () => {
  return (
    <Layout>
      <BackButton />
     {/* TODO:  <VectorReflection /> */}
    </Layout>
  );
};
